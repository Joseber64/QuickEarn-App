// Código para manejar la lista de tareas en la pantalla
class TaskAdapter(private val tasks: List<Task>, private val onClick: (Task) -> Unit) {
    // Aquí el código se encarga de "inflar" cada cuadrito de tarea
    // con su nombre, descripción y el monto en pesos ($).
}
